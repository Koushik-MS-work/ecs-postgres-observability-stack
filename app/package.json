{
  "name": "devops-project-app",
  "version": "1.0.0",
  "description": "Sample Node.js/Express service used to demonstrate the CI/CD pipeline, container build, and ECS deployment.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "lint": "eslint src --ext .js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
